<Window x:Class="HotAlert.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:HotAlert.Converters"
        xmlns:vm="clr-namespace:HotAlert.ViewModels"
        Title="HotAlert 设置"
        Width="450" Height="580"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Window.Resources>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>

        <!-- 分组标题样式 -->
        <Style x:Key="GroupHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <!-- 标签样式 -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <!-- 颜色块样式 -->
        <Style x:Key="ColorBlockStyle" TargetType="Border">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#666"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 阈值设置 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="阈值设置" Style="{StaticResource GroupHeaderStyle}"/>

                        <!-- CPU 阈值 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="CPU 阈值" Style="{StaticResource LabelStyle}"/>
                            <Slider Grid.Column="1"
                                    Value="{Binding CpuThreshold}"
                                    Minimum="10" Maximum="100"
                                    TickFrequency="5" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding CpuThreshold, StringFormat={}{0}%}"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>

                        <!-- 内存阈值 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="内存阈值" Style="{StaticResource LabelStyle}"/>
                            <Slider Grid.Column="1"
                                    Value="{Binding MemoryThreshold}"
                                    Minimum="10" Maximum="100"
                                    TickFrequency="5" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding MemoryThreshold, StringFormat={}{0}%}"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 边框设置 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="边框设置" Style="{StaticResource GroupHeaderStyle}"/>

                        <!-- 最小宽度 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="最小宽度" Style="{StaticResource LabelStyle}"/>
                            <Slider Grid.Column="1"
                                    Value="{Binding BorderMinWidth}"
                                    Minimum="5" Maximum="30"
                                    TickFrequency="1" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding BorderMinWidth, StringFormat={}{0}px}"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>

                        <!-- 最大宽度 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="最大宽度" Style="{StaticResource LabelStyle}"/>
                            <Slider Grid.Column="1"
                                    Value="{Binding BorderMaxWidth}"
                                    Minimum="20" Maximum="100"
                                    TickFrequency="5" IsSnapToTickEnabled="True"
                                    VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding BorderMaxWidth, StringFormat={}{0}px}"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 颜色设置 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="颜色设置" Style="{StaticResource GroupHeaderStyle}"/>

                        <!-- CPU 颜色 -->
                        <StackPanel Margin="0,0,0,15">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="CPU 警告颜色" Style="{StaticResource LabelStyle}"/>
                                <Border Width="24" Height="24" CornerRadius="4"
                                        Background="{Binding CpuColor, Converter={StaticResource ColorToBrushConverter}}"
                                        BorderBrush="#CCC" BorderThickness="1"/>
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding PresetColors}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ColorBlockStyle}"
                                                Background="{Binding Converter={StaticResource ColorToBrushConverter}}">
                                            <Border.InputBindings>
                                                <MouseBinding MouseAction="LeftClick"
                                                              Command="{Binding DataContext.SelectCpuColorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                              CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <!-- 内存颜色 -->
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="内存警告颜色" Style="{StaticResource LabelStyle}"/>
                                <Border Width="24" Height="24" CornerRadius="4"
                                        Background="{Binding MemoryColor, Converter={StaticResource ColorToBrushConverter}}"
                                        BorderBrush="#CCC" BorderThickness="1"/>
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding PresetColors}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ColorBlockStyle}"
                                                Background="{Binding Converter={StaticResource ColorToBrushConverter}}">
                                            <Border.InputBindings>
                                                <MouseBinding MouseAction="LeftClick"
                                                              Command="{Binding DataContext.SelectMemoryColorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                              CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 动画设置 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="动画设置" Style="{StaticResource GroupHeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="呼吸灯速度" Style="{StaticResource LabelStyle}"/>
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding BreathSpeedOptions}"
                                      SelectedValue="{Binding BreathSpeed}"
                                      SelectedValuePath="Value"
                                      DisplayMemberPath="DisplayName"
                                      Width="120" HorizontalAlignment="Left"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 系统设置 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="系统设置" Style="{StaticResource GroupHeaderStyle}"/>
                        <CheckBox Content="开机自启动"
                                  IsChecked="{Binding AutoStart}"
                                  VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- 底部操作 -->
                <Button Content="恢复默认设置"
                        Command="{Binding ResetCommand}"
                        HorizontalAlignment="Left"
                        Padding="15,8"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
