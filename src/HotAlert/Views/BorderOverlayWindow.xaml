<Window x:Class="HotAlert.Views.BorderOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HotAlert Border"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        IsHitTestVisible="False">

    <Window.Resources>
        <!-- 呼吸灯动画 -->
        <Storyboard x:Key="BreathAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="BorderContainer"
                           Storyboard.TargetProperty="Opacity"
                           From="0.7" To="1.0" Duration="0:0:1"/>
        </Storyboard>
    </Window.Resources>

    <Grid x:Name="BorderContainer">
        <!-- CPU 边框（外层） -->
        <Grid x:Name="CpuBorderGrid" Visibility="Collapsed">
            <Rectangle x:Name="CpuTopBorder" VerticalAlignment="Top"/>
            <Rectangle x:Name="CpuBottomBorder" VerticalAlignment="Bottom"/>
            <Rectangle x:Name="CpuLeftBorder" HorizontalAlignment="Left"/>
            <Rectangle x:Name="CpuRightBorder" HorizontalAlignment="Right"/>
        </Grid>

        <!-- 内存边框（中层） -->
        <Grid x:Name="MemoryBorderGrid" Visibility="Collapsed">
            <Rectangle x:Name="MemoryTopBorder" VerticalAlignment="Top"/>
            <Rectangle x:Name="MemoryBottomBorder" VerticalAlignment="Bottom"/>
            <Rectangle x:Name="MemoryLeftBorder" HorizontalAlignment="Left"/>
            <Rectangle x:Name="MemoryRightBorder" HorizontalAlignment="Right"/>
        </Grid>

        <!-- 温度边框（最内层） -->
        <Grid x:Name="TemperatureBorderGrid" Visibility="Collapsed">
            <Rectangle x:Name="TemperatureTopBorder" VerticalAlignment="Top"/>
            <Rectangle x:Name="TemperatureBottomBorder" VerticalAlignment="Bottom"/>
            <Rectangle x:Name="TemperatureLeftBorder" HorizontalAlignment="Left"/>
            <Rectangle x:Name="TemperatureRightBorder" HorizontalAlignment="Right"/>
        </Grid>

        <!-- 悬浮提示 -->
        <Popup x:Name="TooltipPopup"
               Placement="Mouse"
               AllowsTransparency="True"
               IsHitTestVisible="False">
            <Border Background="#DD000000"
                    CornerRadius="4"
                    Padding="8,4">
                <TextBlock x:Name="TooltipText"
                          Foreground="White"
                          FontSize="12"/>
            </Border>
        </Popup>
    </Grid>
</Window>
